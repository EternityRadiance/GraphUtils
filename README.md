## 🚀 Быстрый запуск

### Windows
1. Скачайте проект
2. **Кликните дважды** на `run.bat` или введите в `cmd.exe`.
3. Готово!

### Linux / Mac
```bash
# Сделайте скрипт исполняемым (один раз)
chmod +x run.sh

# Запускайте
./run.sh
```
